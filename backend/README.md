### STEP 1: cd to backend directory
`cd backend`

### STEP 2: install docker desktop
https://www.docker.com/products/docker-desktop/

### STEP 3: run docker file
`docker compose up -d`


### STEP 4: import collection.json into postman to test APIs
